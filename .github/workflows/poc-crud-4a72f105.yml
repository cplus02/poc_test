name: POC CRUD Test
on:
  workflow_dispatch:
    inputs:
      tcrt_run_id:
        description: 'TCRT correlation ID'
        required: false
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from POC CRUD v1"
